version: '3.8'

services:
  remove-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: remove-manga-separation
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
